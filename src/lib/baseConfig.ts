import { Config } from "./type";

const config: Config = {
  templateSuffix: ".html",
  inputFolder: "input",
  outputFolder: "output",
  variablesType: "json",
  locales: ["en"],
};

export default config;
